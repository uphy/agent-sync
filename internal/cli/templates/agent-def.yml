configVersion: "1.0"

projects:
  my-project:
    outputDirs:
      - ./dest # Example destination
    tasks:
      - name: "Project Memory"
        type: memory
        inputs:
          - ./memories/
        outputs:
          - agent: claude
          - agent: roo

      - name: "Project Commands"
        type: command
        inputs:
          - ./commands/
        outputs:
          - agent: claude
          - agent: roo

user:
  tasks: []
